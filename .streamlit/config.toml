# Theme settings are commented out to allow users to customize via Streamlit Cloud settings
# Uncomment these lines if you want to force a specific theme
# [theme]
# primaryColor = "#FF6B6B"
# backgroundColor = "#0E1117"
# secondaryBackgroundColor = "#262730"
# textColor = "#FAFAFA"
# font = "sans serif"

[server]
headless = true
enableCORS = false
enableXsrfProtection = true
maxUploadSize = 200
# Aggressive memory cleanup: clean up disconnected sessions after 10 seconds
# Default is ~10-15 minutes. Users who accidentally close tab have 10s to reopen.
# This prevents memory buildup from reload cycles (load book → save → reload → repeat)
disconnectedSessionTTL = 10

[browser]
gatherUsageStats = false

[client]
toolbarMode = "viewer"
showErrorDetails = true
